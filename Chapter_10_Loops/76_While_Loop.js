let attempt = 0; //initilization
while (attempt < 3) {
    console.log(attempt); //condition check
    attempt++; //increment
}

let modi = 1;
while (modi <= 15) {
    console.log(`Modi will do ${modi}+ years`);
    modi++; //increment
}